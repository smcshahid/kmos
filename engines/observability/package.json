{
  "name": "@kmos/observability",
  "version": "1.0.0-pc.2",
  "private": true,
  "type": "module",
  "description": "Zero-dependency observability toolkit (metrics, structured logging, health) every KMOS service uses to expose health/metrics/logs (KMOS-0200 \u00a713, KMOS-9999 \u00a718). Deterministic: clocks/sinks are injected.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@kmos/canonical-kernel": "*"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "node --experimental-strip-types --import ../../tools/dev/register.mjs --test test/*.test.ts"
  }
}
