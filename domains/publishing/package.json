{
  "name": "@kmos/publishing",
  "version": "1.0.0-pc.2",
  "private": true,
  "type": "module",
  "description": "KMOS Publishing domain service (KMOS-0002 Publishing): composes a deterministic metadata-generation capability via the Workflow + Runtime, packages a Publication asset with lineage to its sources, and releases only after a Governance approval. Holds no business logic of its own (it lives in capabilities).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@kmos/canonical-kernel": "*",
    "@kmos/assets": "*",
    "@kmos/governance": "*",
    "@kmos/capability-registry": "*",
    "@kmos/capability-runtime": "*",
    "@kmos/workflow": "*"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "node --experimental-strip-types --import ../../tools/dev/register.mjs --test test/*.test.ts"
  }
}
