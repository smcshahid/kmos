apiVersion: v1
kind: Service
metadata:
  name: podcast-studio
  namespace: {{ .Release.Namespace }}
  labels:
    app: podcast-studio
spec:
  selector:
    app: podcast-studio
  ports:
    - name: http
      port: 8091
      targetPort: http
