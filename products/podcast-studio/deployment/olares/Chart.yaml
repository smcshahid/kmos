apiVersion: v2
name: podcast-studio
description: Podcast Studio — second flagship KMOS ecosystem app (Olares Application Chart). Companion to the KMOS deployment; shares its PostgreSQL event log.
type: application
# Chart version (bump on packaging changes); appVersion tracks the Podcast Studio release.
version: 1.1.1
appVersion: "1.1.1"
