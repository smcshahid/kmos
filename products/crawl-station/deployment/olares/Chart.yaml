apiVersion: v2
name: crawl-station
description: CrawlStation — the KMOS ecosystem acquisition front-end (Olares Application Chart). Companion to the KMOS deployment; shares its PostgreSQL event log.
type: application
# Chart version (bump on packaging changes); appVersion tracks the CrawlStation release.
version: 1.0.0
appVersion: "1.0.0"
