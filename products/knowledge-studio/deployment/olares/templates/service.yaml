apiVersion: v1
kind: Service
metadata:
  name: knowledge-studio
  namespace: {{ .Release.Namespace }}
  labels:
    app: knowledge-studio
spec:
  selector:
    app: knowledge-studio
  ports:
    - name: http
      port: 8090
      targetPort: http
