apiVersion: v2
name: knowledge-studio
description: Knowledge Studio — flagship KMOS ecosystem app (Olares Application Chart). Companion to the KMOS deployment; shares its PostgreSQL event log.
type: application
# Chart version (bump on packaging changes); appVersion tracks the Knowledge Studio release.
version: 1.0.0
appVersion: "1.0.0"
