{
  "name": "@kmos/reference-capabilities",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "description": "Reference KMOS capabilities (deterministic stubs) implementing the CapabilityHandler contract structurally. Real engines (Whisper, ffmpeg, LLMs) slot behind the same contract later (KMOS-0120 \u00a713/\u00a722).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@kmos/canonical-kernel": "*"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "node --experimental-strip-types --import ../../tools/dev/register.mjs --test test/*.test.ts"
  }
}
