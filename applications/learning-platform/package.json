{
  "name": "@kmos/learning-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Learning Platform (KMOS-0009): a THIN application that assembles institutional knowledge into curricula and lessons. Lessons present authoritative Knowledge; the app owns no business logic and no canonical objects (progress is ephemeral session state).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@kmos/canonical-kernel": "*",
    "@kmos/knowledge": "*"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "node --experimental-strip-types --import ../../tools/dev/register.mjs --test test/*.test.ts"
  }
}
