apiVersion: v1
kind: Service
metadata:
  name: kmos
  namespace: {{ .Release.Namespace }}
  labels:
    app: kmos
spec:
  selector:
    app: kmos
  ports:
    - name: http
      port: 8080
      targetPort: http
