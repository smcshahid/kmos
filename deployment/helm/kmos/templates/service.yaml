apiVersion: v1
kind: Service
metadata:
  name: {{ include "kmos.fullname" . }}
  labels:
    {{- include "kmos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kmos.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
