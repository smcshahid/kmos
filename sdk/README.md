# sdk/
Developer SDK: client libraries, capability/extension templates, and developer
tooling (KMOS-10020). The first SDK assets (capability template + scaffolding)
are a planned consultancy-recommended deliverable; the authoritative contracts an
SDK must satisfy are defined by `packages/conformance` (the KMOS Conformance Kit).
